# sample xbindkeys config
# place this file at ~/.xbindkeysrc

# have to run this in terminal
# make xbindkeys reload config
# killall -s1 xbindkeys
# start xbindkeys daemon
# xbindkeys -f ~/.xbindkeysrc

# # To swicth main used applications
# "wmctrl -xa inkscape || inkscape"
# Control+Mod2 + F1

# "wmctrl -xa evince || evince"
# Control+Mod2 + F2

# "wmctrl -xa emacs || emacs"
# Control+Mod2 + F3

# "wmctrl -xa gnome-terminal || gnome-terminal"
# Control+Mod2 + F4

# "wmctrl -xa chromium-browser || chromium-browser"
# Control+Mod2 + F5

# # in firefox, copy page url and go to emacs
# "xvkbd -no-jump-pointer -xsendevent -text '\Cl' ; xvkbd -no-jump-pointer -xsendevent -text '\Cc' ; wmctrl -xa emacs"
# F6

# # in emacs, do C-x 
# "xvkbd -no-jump-pointer -xsendevent -text '\Cx'"
# F2

# --- text above must be commented if global configuration uses i3 window manager
# compatible with i3 window manager
# do C-c (for emacs use) (m:0x14 + c:55 matches with C-return in my keyboard-layout)
"xvkbd -no-jump-pointer -xsendevent -text '\Cc'"
m:0x14 + c:55